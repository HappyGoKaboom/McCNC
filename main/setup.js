/*
    Initial setup script
 */

const fs = require("fs");

module.exports = {
    run: function () {
        // check if setup is already done

        // check if config already exist

        // if so user ask the user to import them

        // if user data folder already exists ask to use it

        // if config do not exists create config.json

        // create user data folder

        // find minecraft folders
    }
};